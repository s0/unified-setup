#! /bin/bash

background_dir=/unified-setup/opt/desktop/wallpapers
background=$( ls $background_dir | sort -R | head -n 1)
#feh --no-xinerama --bg-fill $background_dir/$background
feh --no-xinerama --bg-tile $background_dir/$background
